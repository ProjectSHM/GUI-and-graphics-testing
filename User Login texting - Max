from Tkinter import *
import pickle

root = Tk()

def login():
    user_label = Label(root, text="User Name:")
    user_label.grid(row=0, column=0, sticky=E)

    user_entry = Entry(root)
    user_entry.grid(row=0, column=1)

    password = Label(root, text="Password:")
    password.grid(row=1, column=0, sticky=E)
    
    password_entry = Entry(root, show="*")
    password_entry.grid(row=1, column=1)

    login_button= Button(root, text="Login", command=lambda:user_details())
    login_button.grid(row=2, column=0, columnspan=2)

    def user_details():
        user_dict[user_entry.get()] = password_entry.get()
        print str(user_entry.get())
        print str(password_entry.get())
        print user_dict

    def user_save():
        
        
    user_dict ={}
    
login()    
root.mainloop()
